# せんごく タイピング

小学2年生（キーボード触りたて・ローマ字未習）が、楽しくブラインドタッチの素地を身につけるためのブラウザゲーム。

**戦国時代にまつわる言葉（武将・武具・用語）の名前をローマ字で打つ**と、その武将や武具が あらわれて「ずかん」に登録される、集める育成型。`oda`→織田、`katana`→刀、`nobunaga`→信長 のように、打った名前がそのまま仲間になる。

## あそびかた

- メニューの「ゆびの たいそう」でホームポジション（asdf / jkl;）に慣れる
- 「せんごくの ことばを あつめる」でステージを選び、出てきた言葉をローマ字で打つ
- 正しく打つと その武将/武具が あらわれて ずかんに登録。ステージを全部あつめると次が解放
- 画面のキーボードは**指ごとに色分け**。次に押すキーが光るので、**ローマ字を知らなくても光に従えば打てる**（手元を見ずに画面で打つ＝ブラインドタッチの素地、小3のローマ字入力の先取り）
- 武将名・用語は漢字に**ふりがな**を振り、日本語で**読み上げ**もする
- 「おたすけ」で補助を3段階に調整：しょしんしゃ（点滅＋色）→ なれてきた（色のみ）→ たつじん（キーボードをかくす）

## 開発

```bash
npm install
npm run dev     # 開発サーバー（http://localhost:5173/）
npm run test    # ユニットテスト（vitest）
npm run build   # 型チェック＋本番ビルド（dist/）
npm run preview # ビルド成果物をプレビュー
```

ロジック（`src/core`）と画面（`src/ui`）を分離。打鍵判定・指割当・進捗保存は純粋関数で単体テスト済み。集める言葉は `src/data/words.ts`（テーマ非依存の `GameWord`）にまとまっており、入れ替え・追加が容易。

## デプロイ（GitHub Pages）

`main` に push すると `.github/workflows/deploy.yml` が `dist/` を GitHub Pages へ公開する。
初回のみ GitHub リポジトリの **Settings → Pages → Source: GitHub Actions** を有効化する。
`vite.config.ts` の `base: "./"` によりサブパス配信でもそのまま動く。
