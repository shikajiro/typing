# どうぶつタイピングずかん

小学2年生（キーボード触りたて・ローマ字未習）が、楽しくブラインドタッチの素地を身につけるためのブラウザゲーム。

**英単語＝どうぶつの名前**を打つと、たまごがかえって「ずかん」に仲間入り。打って集める育成・コレクション型。

## あそびかた

- メニューの「ゆびの たいそう」でホームポジション（asdf / jkl;）に慣れる
- 「どうぶつを あつめる」でワールドを選び、出てきた英単語（`cat` など）を打つ
- 正しく打つと たまごがかえって ずかんに登録。ワールドを全部あつめると次が解放
- 画面のキーボードは**指ごとに色分け**。次に押すキーが光る（手元を見ずに画面で打つ＝ブラインドタッチの素地）
- 「おたすけ」で補助を3段階に調整：しょしんしゃ（点滅＋色）→ なれてきた（色のみ）→ たつじん（キーボードをかくす）

## 開発

```bash
npm install
npm run dev     # 開発サーバー（http://localhost:5173/）
npm run test    # ユニットテスト（vitest）
npm run build   # 型チェック＋本番ビルド（dist/）
npm run preview # ビルド成果物をプレビュー
```

ロジック（`src/core`）と画面（`src/ui`）を分離。打鍵判定・指割当・進捗保存は純粋関数で単体テスト済み。

## デプロイ（GitHub Pages）

`main` に push すると `.github/workflows/deploy.yml` が `dist/` を GitHub Pages へ公開する。
初回のみ GitHub リポジトリの **Settings → Pages → Source: GitHub Actions** を有効化する。
`vite.config.ts` の `base: "./"` によりサブパス配信でもそのまま動く。
